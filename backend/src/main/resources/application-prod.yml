spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0}
    username: ${SPRING_DATASOURCE_USERNAME:postgres.xvlvpqmrtxvxxzkpxhqs}
    password: ${SPRING_DATASOURCE_PASSWORD:Surayu@6299}
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      idle-timeout: 300000
      connection-timeout: 20000

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        temp:
          use_jdbc_metadata_defaults: false

  cache:
    type: none
  session:
    store-type: none

  data:
    redis:
      repositories:
        enabled: false

logging:
  level:
    com.gymora: INFO
    org.springframework.security: WARN

app:
  jwt:
    secret: ${JWT_SECRET:gymora-super-secret-key-that-should-be-at-least-256-bits-long-for-security}
  cors:
    allowed-origins: "*"
